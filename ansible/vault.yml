# ================================================================
#  BANCONOVA — Variables sensibles (cifrar con ansible-vault)
#
#  INSTRUCCIONES:
#  1. Editar este archivo con valores reales
#  2. Cifrarlo: ansible-vault encrypt vault.yml
#  3. Usarlo en el playbook: ansible-playbook site.yml --ask-vault-pass
#
#  NUNCA subir este archivo sin cifrar a Git
# ================================================================
---

# Clave secreta de la aplicación Flask
secret_key: "cambia-esta-clave-segura-en-produccion-2026"

# Credenciales de base de datos
db_password: "cambia-esta-contrasena-en-produccion"

# Token del registry de Docker (GitHub Container Registry)
registry_token: "ghp_token_de_github_aqui"

# Usuario del registry
registry_usuario: "banconova"
